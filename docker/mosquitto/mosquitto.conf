per_listener_settings false

listener 1883

listener 9001
protocol websockets

# Auth — overridden by entrypoint script.
# If passwd file exists → password_file is used, anonymous disabled.
# Otherwise → anonymous allowed (dev mode).
allow_anonymous true
